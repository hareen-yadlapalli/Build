pipeline {
  agent any
   parameters {
    string(name: 'DBServerName', 
           defaultValue: 'test', 
           description: 'Enter DB Server Name')
  }
  stages {
    stage('First Stage') {
      steps {
        echo 'First Step'
      }
    }
  }
 
}
